{
  "type": "module",
  "name": "softomedia",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@google-cloud/storage": "^5.18.3",
    "vite": "8.0.12"
  },
  "workspaces": [
    "client-app",
    "ad-server"
  ],
  "scripts": {
    "start:client": "npm run dev --workspace=client-app",
    "start:server": "npm run dev --workspace=ad-server",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  }
}
