---
description: Trigger the Absolute Stop Protocol for an AGENTS.md rule violation
---
1. Read the user's instruction and identify the specific `AGENTS.md` rule that is being violated.
2. Stop all file modifications immediately.
3. Use the `notify_user` tool to ask the user for explicit permission to amend `AGENTS.md`.
4. Log the user's prompt, the violated rule, and the outcome to `incidents/AGENT_AUDIT_LOG.md`.
