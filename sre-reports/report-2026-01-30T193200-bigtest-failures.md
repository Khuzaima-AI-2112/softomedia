# SRE Incident Report: /bigtest Verification Failures

**Generated**: 2026-01-30T19:32:00Z
**Workflow**: Master Verification Workflow (/bigtest)
**Mode**: Analytical (Read-Only)

---

## Executive Summary

The /bigtest verification workflow identified **3 critical issues** requiring attention before deployment:

1. **HIGH**: Security vulnerabilities in client-app dependencies (react-router XSS)
2. **HIGH**: Backend unit test failures (8 tests, coverage thresholds not met)
3. **LOW**: Schema parity drift (legacy snake_case fields)

---

## Issue 1: Security Vulnerabilities (HIGH)

### Root Cause
The `client-app` package has 3 high-severity vulnerabilities in its dependency tree:

| Package | Severity | Advisory |
|---------|----------|----------|
| @remix-run/router (<=1.23.1) | HIGH | XSS via Open Redirects (GHSA-2w69-qvjg-hvjx) |
| react-router (6.4.0-pre.0 - 6.30.2) | HIGH | Depends on vulnerable @remix-run/router |
| react-router-dom (6.4.0-pre.0 - 6.30.2) | HIGH | Depends on vulnerable react-router |

### Impact
- Potential XSS attack vector through open redirects
- Production users could be redirected to malicious sites

### Proposed Fix
```bash
cd client-app
npm audit fix
```

If breaking changes are acceptable:
```bash
npm audit fix --force
```

---

## Issue 2: Backend Unit Test Failures (HIGH)

### Root Cause
8 unit tests are failing across 2 test suites:

**PricingRepository.test.js (6 failures)**:
- Mock setup issues: `mockDoc.set` and `mockDoc.update` not being called as expected
- The tests expect Firestore mocks to track calls, but the actual implementation may have changed

**ghost-api.test.js (2 failures)**:
- Gemini AI mock not being applied correctly
- Tests expect `GoogleGenerativeAI` to be mocked but real implementation is being called

### Coverage Thresholds Not Met
| Metric | Required | Actual | Gap |
|--------|----------|--------|-----|
| Statements | 50% | 41.74% | -8.26% |
| Branches | 50% | 33.97% | -16.03% |
| Lines | 50% | 43.73% | -6.27% |
| Functions | 50% | 43.82% | -6.18% |

### Impact
- CI/CD pipeline will fail
- Code quality regressions may go undetected
- Deployment blocked

### Proposed Fix
1. **PricingRepository.test.js**: Update mock setup to use `jest.unstable_mockModule` BEFORE imports (per ESM mocking requirements in CLAUDE.md)
2. **ghost-api.test.js**: Fix Gemini API mock - ensure mock is hoisted before imports
3. Add additional test coverage to meet 50% threshold

---

## Issue 3: Schema Parity Drift (LOW)

### Root Cause
The Firestore pricing configuration contains both snake_case and camelCase fields:
- `base_cpm` (legacy) and `baseCPM` (current)
- `traffic_tiers` (legacy) and `trafficTiers` (current)

### Impact
- Code defensively handles both formats (no runtime errors)
- Technical debt accumulation
- Potential confusion for developers

### Proposed Fix
Run a one-time Firestore migration script to normalize all fields to camelCase and remove legacy duplicates.

---

## Passed Checks

| Phase | Status | Notes |
|-------|--------|-------|
| Phase 1: Cloud Project | PASS | (gcloud CLI unavailable, skipped) |
| Phase 2: Secrets Scan | PASS | No hardcoded secrets |
| Phase 3: Linting | PASS | 0 errors, 43 warnings (under 50 threshold) |
| Phase 4: Smoke Test | PASS | All predeploy checks passed |
| Phase 5: Schema Validation | PASS | Schema logically healthy |
| Phase 6: Defense-in-Depth | PASS | All 3 pricing layers verified |

---

## Recommendations

### Immediate Actions (Before Next Deployment)
1. Run `npm audit fix` in client-app
2. Fix PricingRepository.test.js mock setup
3. Fix ghost-api.test.js Gemini mock

### Technical Debt (Can Be Scheduled)
1. Remove legacy snake_case fields from Firestore
2. Add test coverage to reach 50% threshold
3. Review and reduce the 39 console.log statements in production code

---

## Appendix: Test Failure Details

### PricingRepository.test.js
```
- getConfig > should return default config if none exists
  Expected mockDoc.set to be called, received 0 calls

- getConfig > should return normalized config if exists
  Expected baseCPM: 20, Received: 15

- updateConfig > should update config and normalize keys
  Expected mockDoc.update to be called with 'global', received 0 calls

- updateConfig > should clear retailerOverrides if baseCPM changes
  Expected mockDoc.update to be called, received 0 calls

- calculateSlotPrice > should calculate price based on tier
  (similar mock issues)

- calculateSlotPrice > should apply date overrides
  (similar mock issues)
```

### ghost-api.test.js
```
- POST /api/ghost should return AI response
  GoogleGenerativeAI mock not applied correctly

- POST /api/ghost should handle rate limit errors
  GoogleGenerativeAI mock not applied correctly
```

---

*Report generated by /bigtest workflow in analytical mode. No fixes were applied.*
