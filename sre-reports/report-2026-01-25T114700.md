# SRE Incident Report
**Date**: 2026-01-25
**Workflow**: /bigtest

## Failure Description
Phase 3 (Operational Hygiene) failed during the `/bigtest` workflow execution.
The command `npm run lint` failed with the error: `npm error Missing script: "lint"`.

## Root Cause
The root `package.json` file does not contain a "lint" script definition.
While `ad-server` and `client-app` likely have their own linting configurations, the root-level aggregation script expected by the `/bigtest` workflow is missing.

## Impact
- **Blocking**: The Master Verification Workflow (`/bigtest`) cannot complete successfully.
- **Hygiene**: Unable to verify code quality and log sanitation standards at the project level.

## Proposed Fix
1.  **Analyze Sub-projects**: Confirm `lint` scripts exist in `ad-server/package.json` and `client-app/package.json`.
2.  **Update Root package.json**: Add a `lint` script that orchestrates linting for both sub-projects.
    *   *Draft Command*: `"lint": "cd ad-server && npm run lint && cd ../client-app && npm run lint"`
3.  **Validation**: Re-run Phase 3 of `/bigtest` to verify.

## Evidence
Contents of `package.json` "scripts" block:
```json
"scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "test:e2e": "playwright test",
  "test:e2e:ui": "playwright test --ui",
  "test:e2e:headed": "playwright test --headed",
  "db:seed": "cd ad-server && npm run seed"
}
```
*Note: "lint" is absent.*
