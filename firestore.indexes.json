{
  "_comment": [
    "Sprint 16 — S16-3: composite indexes for invoices and loops collections.",
    "Sprint 22 — S22-2: composite index for retailers collection.",
    "  deleted_at + status — required by GET /api/retailers?for=campaign (S21-4).",
    "  Without this index, Firestore throws FAILED_PRECONDITION under load (RISK-S22-2).",
    "  Field names confirmed from ad-server/src/api/retailers.js (?for=campaign branch).",
    "Field names confirmed from ad-server/src/api/invoices.js (Pre-check A):",
    "  advertiserId, generatedAt — camelCase (inline InvoiceRepository in invoices.js).",
    "Loop field names confirmed from ad-server/src/repositories/LoopRepository.js.",
    "",
    "Pre-check B: firebase.json is absent from this repo. To deploy these indexes:",
    "  Option A — add a firebase.json first, then run:",
    "    firebase deploy --only firestore:indexes --project <your-project-id>",
    "  Option B — deploy directly without firebase.json:",
    "    firebase firestore:indexes --project <your-project-id> < firestore.indexes.json",
    "  Option C — via Google Cloud Console: Firestore > Indexes > Add composite index.",
    "",
    "Staging deploy must precede production. Check for HTTP 409 (index already exists)",
    "on staging — safe to ignore if field names and order match exactly."
  ],
  "indexes": [
    {
      "collectionGroup": "invoices",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "advertiserId", "order": "ASCENDING" },
        { "fieldPath": "generatedAt",  "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "invoices",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "advertiserId", "order": "ASCENDING" },
        { "fieldPath": "amount",        "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "loops",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "retailer_id", "order": "ASCENDING" },
        { "fieldPath": "status",      "order": "ASCENDING" },
        { "fieldPath": "date",        "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "loops",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "location_id", "order": "ASCENDING" },
        { "fieldPath": "status",      "order": "ASCENDING" },
        { "fieldPath": "date",        "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "loops",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "status", "order": "ASCENDING" },
        { "fieldPath": "date",   "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "retailers",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "deleted_at", "order": "ASCENDING" },
        { "fieldPath": "status",     "order": "ASCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
