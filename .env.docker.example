# Copy to .env.docker (already git-ignored) before running the Docker demo.
# See README.md "Run the demo with Docker".

# Chosen locally by whoever runs the demo. At least 12 characters.
# Never commit the real value.
DEMO_ACCOUNT_PASSWORD=
