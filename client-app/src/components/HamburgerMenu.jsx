// HamburgerMenu.jsx has been removed.
// Persona switching → PersonaSwitcher.jsx (top-bar)
// User identity + logout → avatar dropdown in DashboardLayout.jsx
// Nav links (Dashboard, Demo Player, Health) → URL-accessible directly
//   until a permanent nav solution is implemented.
//
// This file is intentionally left as a tombstone to prevent stale imports
// from causing silent failures. Safe to delete once confirmed no references remain.
export default null;
