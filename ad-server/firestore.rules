rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all public access by default
    // Note: Admin SDK (used by ad-server) bypasses these rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
